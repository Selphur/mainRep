1. Fill in the variables in the mysql-connect.php file.
2. Either create or import the database, the name of which you have specified in the mysql-connect.php file (variable "$dbname").
3. Make sure that all the files provided are in the same directory.
4. To launch the programm access the directory with the files.